<h1>feed</h1>

<form action="/posts/<%= @pageuser.id %>" method="post">
  <label>Write something...</label><br>
  <textarea name="content" rows="10" cols="50" maxlength="150" onkeyup="calcChars(this)"></textarea>
  <br>
  <div><span class="postcount">150</span> Characters left</div>
  <input type="submit" value="Share">
</form>

See the posts:
<% @posts.reverse_each do |post| %>
  <div class="post-box">
    <p>Post:</p>
    <p><%= post.content %> </p>
    <p>Posted by: <a href="/profile/<%=User.find_by_id(post.user_id).id%>"><%= User.find_by_id(post.user_id).username %></a> </p>
    <p><small><em>Posted on: <%= (post.created_at).localtime.strftime("%B %d, %Y %I:%M:%S %p") %></em></small></p>
      <% if @pageuser.id == post.user_id %>
        <small><a href="/postsedit/<%=post.id%>">Edit</a></small>
      <% end %>
  </div>
<% end %>
